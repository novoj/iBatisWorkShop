<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
        "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="cz.novoj.ibatis.ProductGroupMapper">

    <insert id="createProductGroup" parameterType="group" keyProperty="id">
        insert into productGroup (name, groupType)
               values (#{name}, #{groupType})
    </insert>

    <update id="updateProductGroup" parameterType="group">
        update productGroup
            set name = #{name},
                groupType = #{groupType}
            where id = #{id}
    </update>

</mapper>
